---
phase: 05-postgresql-and-migration-plan
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - docker-compose.yml
  - .env.example
  - postgres/init/01-create-databases.sh
  - documentation/postgres-deployment.md
requirements: [DB-01, DB-02, DB-04]
autonomous: true
must_haves:
  truths:
    - "D-04 DB-02: Compose uses one PostgreSQL service with separate logical app and audit databases."
    - "D-11: Compose can initialize local/internal-review app and audit databases."
    - "D-12: Production database setup is documented as DBA-managed or managed-database provisioning."
    - "D-14: PostgreSQL attaches only to the data network and exposes no host ports by default."
    - "D-15: PostgreSQL support does not undo Phase 4 runtime minimization."
---

<objective>
Add Docker Compose PostgreSQL support with separate logical app/audit databases, internal data-network placement, persistent storage, and documented production setup boundaries.
</objective>

<tasks>
<task type="auto">
  <name>Add PostgreSQL service and volume</name>
  <files>docker-compose.yml</files>
  <action>Add a single PostgreSQL service for internal-review Docker runs. Attach it only to `primer-data`, add a named data volume, avoid host `ports`, and wire Django dependency behavior without making non-Docker SQLite workflows depend on Postgres.</action>
  <acceptance_criteria>
    - Compose defines one Postgres service.
    - Postgres has no host port publication.
    - Postgres joins only `primer-data`.
    - Django can depend on the database healthcheck when PostgreSQL is enabled.
    - Existing Nginx and FastAPI service network boundaries remain unchanged.
  </acceptance_criteria>
  <verify>python -c "import yaml, pathlib; yaml.safe_load(pathlib.Path('docker-compose.yml').read_text(encoding='utf-8')); print('compose yaml ok')"</verify>
</task>

<task type="auto">
  <name>Add initialization script</name>
  <files>postgres/init/01-create-databases.sh</files>
  <action>Create an idempotent Postgres container init script that creates the separate app and audit logical databases and users for local/internal-review Docker. Use placeholders from environment variables and avoid embedding real secrets.</action>
  <acceptance_criteria>
    - Init script creates app and audit databases only on first Postgres volume initialization.
    - Init script uses environment variables for database names, users, and passwords.
    - Init script is safe to read in Git and contains no real credentials.
    - Failure mode is explicit when required local/internal-review variables are missing.
  </acceptance_criteria>
  <verify>Get-Content postgres\init\01-create-databases.sh</verify>
</task>

<task type="auto">
  <name>Document deployment environment contract</name>
  <files>.env.example, documentation/postgres-deployment.md</files>
  <action>Document the opt-in Docker Postgres env vars, app/audit database separation, local/internal-review initialization flow, and production DBA/managed-database expectations.</action>
  <acceptance_criteria>
    - `.env.example` includes placeholder variables needed by Compose and Django.
    - Documentation explains that Compose init is not production secret management.
    - Documentation explains no host database ports by default.
    - Documentation lists commands for migrating both `default` and `audit` databases.
  </acceptance_criteria>
  <verify>rg -n "DATABASE_ENGINE|POSTGRES|AUDIT" .env.example documentation\postgres-deployment.md</verify>
</task>
</tasks>

<verification>
- [ ] `python -c "import yaml, pathlib; yaml.safe_load(pathlib.Path('docker-compose.yml').read_text(encoding='utf-8')); print('compose yaml ok')"`
- [ ] Structural check that Postgres is on `primer-data` only and has no `ports`.
- [ ] `python django-app\manage.py check`
- [ ] `docker compose config` where Docker is available.
</verification>

<success_criteria>
- [ ] DB-01 has Docker PostgreSQL support.
- [ ] DB-02 has separate app/audit logical databases.
- [ ] DB-04 has documented Docker database configuration.
- [ ] Phase 3 network segmentation constraints remain intact.
</success_criteria>
