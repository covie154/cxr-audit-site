---
phase: 05-postgresql-and-migration-plan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - django-app/lunit_audit/settings.py
  - django-app/lunit_audit/tests.py
  - django-app/requirements.txt
  - .env.example
requirements: [DB-01, DB-02, DB-03, DB-04]
autonomous: true
must_haves:
  truths:
    - "D-01 DB-03: SQLite remains the default when PostgreSQL is not explicitly enabled."
    - "D-02 DB-01: The default database can be configured as PostgreSQL through environment variables."
    - "D-04 DB-02: The audit database alias can be configured as a separate PostgreSQL database."
    - "D-13: No real credentials or PHI examples are added to committed files."
---

<objective>
Add environment-driven Django database settings and PostgreSQL runtime dependency support while preserving SQLite as the default local database.
</objective>

<tasks>
<task type="auto">
  <name>Add database config helpers</name>
  <files>django-app/lunit_audit/settings.py</files>
  <action>Refactor database settings into small helper functions that build either SQLite or PostgreSQL config from environment variables. Preserve the existing `default` and `audit` aliases and `AuditRouter` behavior.</action>
  <acceptance_criteria>
    - Absence of PostgreSQL env vars keeps `default` on `BASE_DIR / "db" / "db.sqlite3"`.
    - Absence of audit PostgreSQL env vars keeps `audit` on `BASE_DIR / "db" / "audit.sqlite3"`.
    - Setting `DATABASE_ENGINE=postgres` uses `django.db.backends.postgresql` for `default`.
    - Setting `AUDIT_DATABASE_ENGINE=postgres` or equivalent uses `django.db.backends.postgresql` for `audit`.
    - Connection fields are environment-driven: name, user, password, host, port, optional SSL mode, connection max age, and health checks where practical.
  </acceptance_criteria>
  <verify>python django-app\manage.py check</verify>
</task>

<task type="auto">
  <name>Add focused settings tests</name>
  <files>django-app/lunit_audit/tests.py</files>
  <action>Add tests for SQLite default behavior and PostgreSQL environment-driven behavior for both `default` and `audit` aliases. Use module reload or isolated helper functions so tests do not require a live PostgreSQL server.</action>
  <acceptance_criteria>
    - Tests prove SQLite remains default.
    - Tests prove PostgreSQL config is selected only when explicitly requested.
    - Tests prove app and audit aliases can point to different PostgreSQL database names.
  </acceptance_criteria>
  <verify>python django-app\manage.py test lunit_audit</verify>
</task>

<task type="auto">
  <name>Add PostgreSQL driver dependency and env contract</name>
  <files>django-app/requirements.txt, .env.example</files>
  <action>Add the psycopg 3 dependency to the Django requirements and document database environment variables in `.env.example` with placeholder values only.</action>
  <acceptance_criteria>
    - `django-app/requirements.txt` includes a PostgreSQL driver compatible with Django 5.2.
    - `.env.example` documents opt-in app database variables.
    - `.env.example` documents separate audit database variables.
    - No real secrets, hostnames beyond service names, or PHI examples are committed.
  </acceptance_criteria>
  <verify>python django-app\manage.py check</verify>
</task>
</tasks>

<verification>
- [ ] `python django-app\manage.py check`
- [ ] `python django-app\manage.py test lunit_audit`
- [ ] `python django-app\manage.py makemigrations --check --dry-run`
</verification>

<success_criteria>
- [ ] DB-01 has a PostgreSQL settings path for application data.
- [ ] DB-02 has a separate PostgreSQL settings path for audit data.
- [ ] DB-03 is preserved by SQLite defaults.
- [ ] DB-04 has documented environment variables.
</success_criteria>
