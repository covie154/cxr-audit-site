---
phase: 05-postgresql-and-migration-plan
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - django-app/upload/management/__init__.py
  - django-app/upload/management/commands/__init__.py
  - django-app/upload/management/commands/prepare_postgres_migration.py
  - django-app/upload/management/commands/validate_postgres_migration.py
  - django-app/upload/tests.py
  - documentation/sqlite-to-postgres-migration.md
requirements: [DB-05, DB-06]
autonomous: true
must_haves:
  truths:
    - "D-07 DB-05: Phase 5 includes bounded migration tooling, not only a paper runbook."
    - "D-08 DB-06: Tooling supports export/import/validation using synthetic or explicitly approved non-PHI data."
    - "D-09: Tooling does not perform unsupervised PHI cutover by default."
    - "D-10 DB-06: Validation goes beyond row counts."
    - "D-13: No PHI examples or real secrets are committed."
---

<objective>
Create operator-controlled SQLite-to-PostgreSQL migration tooling and a full runbook covering backup, export/import, validation, rollback, and cutover.
</objective>

<tasks>
<task type="auto">
  <name>Add migration preparation command</name>
  <files>django-app/upload/management/commands/prepare_postgres_migration.py</files>
  <action>Add a Django management command that prepares migration evidence without moving PHI by default. It should report source/target database aliases, migration state, table/model inventory, output paths, and the exact operator commands to run for export/import rehearsal.</action>
  <acceptance_criteria>
    - Command requires explicit flags before writing export artifacts.
    - Default output is a dry-run checklist.
    - Command includes both `default` and `audit` aliases.
    - Command avoids dumping PHI by default and clearly labels any export action as operator-approved.
  </acceptance_criteria>
  <verify>python django-app\manage.py prepare_postgres_migration --dry-run</verify>
</task>

<task type="auto">
  <name>Add migration validation command</name>
  <files>django-app/upload/management/commands/validate_postgres_migration.py</files>
  <action>Add a Django management command that compares source and target migration results using more than row counts. Include migration-state checks, table row counts, representative key comparisons, sampled non-PHI-safe field checks where possible, audit alias checks, and sequence reset guidance.</action>
  <acceptance_criteria>
    - Command supports dry-run/synthetic validation mode without live PostgreSQL.
    - Command can be pointed at source and target aliases.
    - Validation output is structured enough to archive as migration evidence.
    - Validation failures return non-zero unless explicitly run in advisory dry-run mode.
  </acceptance_criteria>
  <verify>python django-app\manage.py validate_postgres_migration --dry-run</verify>
</task>

<task type="auto">
  <name>Add tests for migration commands</name>
  <files>django-app/upload/tests.py</files>
  <action>Add focused tests for dry-run behavior, refusal to export without explicit approval flags, and validation output including both app and audit databases. Avoid live Postgres dependency.</action>
  <acceptance_criteria>
    - Tests do not require Docker or a PostgreSQL server.
    - Tests prove dry-run commands execute successfully.
    - Tests prove unsafe export/cutover actions are not default behavior.
  </acceptance_criteria>
  <verify>python django-app\manage.py test upload</verify>
</task>

<task type="auto">
  <name>Write migration runbook</name>
  <files>documentation/sqlite-to-postgres-migration.md</files>
  <action>Write the full operator runbook for SQLite-to-PostgreSQL migration. Cover backup, rehearsal, schema migration, export/import, validation beyond row counts, sequence reset, freeze/cutover, rollback, audit database validation, and evidence capture.</action>
  <acceptance_criteria>
    - Runbook covers DB-05 end to end.
    - Runbook states validation must use synthetic or approved non-PHI data for rehearsals.
    - Runbook includes rollback and cutover criteria.
    - Runbook separates local/internal-review Compose from production DBA/managed-database responsibilities.
  </acceptance_criteria>
  <verify>rg -n "backup|export|import|validate|rollback|cutover|audit|synthetic|non-PHI" documentation\sqlite-to-postgres-migration.md</verify>
</task>
</tasks>

<verification>
- [ ] `python django-app\manage.py prepare_postgres_migration --dry-run`
- [ ] `python django-app\manage.py validate_postgres_migration --dry-run`
- [ ] `python django-app\manage.py test upload`
- [ ] `python django-app\manage.py test lunit_audit upload`
- [ ] `python django-app\manage.py makemigrations --check --dry-run`
</verification>

<success_criteria>
- [ ] DB-05 is covered by a full runbook and operator-controlled tooling.
- [ ] DB-06 is covered by synthetic/non-PHI validation and checks beyond row counts.
- [ ] The commands are safe by default and do not perform unsupervised PHI migration.
- [ ] Both default and audit database aliases are included in migration guidance.
</success_criteria>
