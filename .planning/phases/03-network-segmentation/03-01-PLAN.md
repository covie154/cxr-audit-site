---
phase: 03-network-segmentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - documentation/HIPAA-plan-phase-3.md
requirements: [NET-01, NET-02, NET-03]
autonomous: true
must_haves:
  truths:
    - "D-01 NET-01: Compose defines public, app, data, and inference networks."
    - "D-02 NET-02: Nginx is attached only to public/app networks."
    - "D-05 NET-03: Django/FastAPI/database services are not exposed on host ports by default."
---

<objective>
Create explicit Docker Compose network topology and service membership.
</objective>

<tasks>
<task type="auto">
  <name>Update Compose networks</name>
  <files>docker-compose.yml</files>
  <action>Replace the single primer-net with primer-public, primer-app, primer-data, and primer-inference. Attach services according to the Phase 3 decisions and preserve current service DNS names.</action>
  <verify>docker compose config</verify>
</task>
</tasks>

<verification>
- [ ] `docker compose config`
</verification>
