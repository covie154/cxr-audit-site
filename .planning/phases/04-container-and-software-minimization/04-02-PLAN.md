---
phase: 04-container-and-software-minimization
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - docker-compose.yml
requirements: [CONT-02, CONT-03]
autonomous: true
must_haves:
  truths:
    - "D-03 CONT-02: Django drops to appuser for application runtime."
    - "D-04 CONT-02: FastAPI runs as appuser."
    - "D-06 CONT-03: Django and FastAPI use no-new-privileges; FastAPI drops capabilities; Django capability drops are deferred because startup volume chown still requires them."
---

<objective>
Add practical Compose execution hardening for Django and FastAPI.
</objective>

<tasks>
<task type="auto">
  <name>Add Compose security options</name>
  <files>docker-compose.yml</files>
  <action>Add no-new-privileges for Django and FastAPI. Add cap_drop ALL for FastAPI only. Avoid unsafe Django/Nginx capability drops unless startup ownership and low-port behavior are verified.</action>
  <verify>docker compose config</verify>
</task>
</tasks>

<verification>
- [ ] `docker compose config`
</verification>
