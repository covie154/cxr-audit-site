---
phase: 04-container-and-software-minimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - django-app/Dockerfile
  - cxr-audit-api/Dockerfile
requirements: [CONT-01]
autonomous: true
must_haves:
  truths:
    - "D-01 CONT-01: Runtime Docker stages do not install gcc/libffi-dev directly."
    - "D-02: Python base remains python:3.11-slim for compatibility."
---

<objective>
Move Django and FastAPI images to multi-stage builds that keep build tools out of runtime stages.
</objective>

<tasks>
<task type="auto">
  <name>Convert Django Dockerfile</name>
  <files>django-app/Dockerfile</files>
  <action>Create builder/runtime stages. Install compiler dependencies only in builder. Copy installed Python packages and scripts into runtime. Preserve entrypoint behavior.</action>
  <verify>docker compose config</verify>
</task>
<task type="auto">
  <name>Convert FastAPI Dockerfile</name>
  <files>cxr-audit-api/Dockerfile</files>
  <action>Create builder/runtime stages. Install compiler dependencies only in builder. Preserve non-root appuser runtime and /app/tmp ownership.</action>
  <verify>docker compose config</verify>
</task>
</tasks>

<verification>
- [ ] `docker compose config`
</verification>
