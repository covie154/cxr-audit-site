---
phase: 01-audit-logging
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - django-app/audit/services.py
  - django-app/audit/tests.py
  - django-app/upload/views.py
  - django-app/gt/views.py
autonomous: true
requirements: [AUD-03, AUD-04, AUD-09, AUD-10]
user_setup: []
must_haves:
  truths:
    - "D-07 D-08 AUD-03 AUD-04: Uploaded files are copied in full to the audit artifact directory with timestamp-prepended sanitized filenames."
    - "D-09 AUD-09: FileUpload audit events link to artifact path, SHA-256 hash, size, filename, and upload context."
    - "D-03 D-04: Artifact metadata avoids direct patient names and raw report text."
    - "D-17 D-18: Upload artifact/audit failures do not block user workflows."
  artifacts:
    - path: "django-app/audit/services.py"
      provides: "Audit artifact copy and hashing helpers"
      contains: "def capture_uploaded_file_artifact"
    - path: "django-app/upload/views.py"
      provides: "Upload and import artifact capture integration"
      contains: "FileUpload"
  key_links:
    - from: "django-app/upload/views.py"
      to: "django-app/audit/services.py"
      via: "capture_uploaded_file_artifact helper"
      pattern: "capture_uploaded_file_artifact"
---

<objective>
Implement full upload artifact capture and FileUpload audit events.

Purpose: Uploads are the entry point for PHI-bearing CARPL/RIS/GT/import files, and the audit trail must preserve reviewable copies with integrity metadata.
Output: Artifact helper functions plus FileUpload integration for analysis uploads, prechecks, historical imports, and manual GT uploads.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit-logging/01-CONTEXT.md
@.planning/phases/01-audit-logging/01-RESEARCH.md
@.planning/phases/01-audit-logging/01-01-SUMMARY.md
@django-app/audit/services.py
@django-app/upload/views.py
@django-app/gt/views.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add audit artifact helpers</name>
  <files>django-app/audit/services.py, django-app/audit/tests.py</files>
  <read_first>django-app/audit/services.py, django-app/lunit_audit/settings.py</read_first>
  <action>Add `capture_uploaded_file_artifact(uploaded_file, category, context)` or equivalent helper. It must read/copy the full uploaded file bytes into `settings.AUDIT_ARTIFACT_ROOT`, prepend a timestamp to a sanitized basename, compute SHA-256 and size, and reset the uploaded file pointer when possible so existing view logic can still read/forward the file. Return artifact metadata suitable for `emit_audit_event`.</action>
  <verify>python django-app/manage.py test audit</verify>
  <acceptance_criteria>
    - Helper creates a real file under `AUDIT_ARTIFACT_ROOT`.
    - Filename begins with a timestamp and contains a sanitized original name.
    - SHA-256 and size are returned.
    - Tests prove file-like object can still be read after capture when seekable.
  </acceptance_criteria>
  <done>Artifact helper is safe and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Capture analysis and precheck uploads</name>
  <files>django-app/upload/views.py, django-app/audit/tests.py</files>
  <read_first>django-app/upload/views.py, django-app/audit/services.py</read_first>
  <action>Instrument `analyze_auto_sort`, `analyze_multiple`, and `precheck_files` so each uploaded file creates a `FileUpload` audit event linked to its copied artifact. Metadata should include route, input field name, sanitized filename, content type, size, SHA-256, supplemental_steps where present, and task ID once available for analyze endpoints. Ensure file bytes are still forwarded to FastAPI exactly once.</action>
  <verify>python django-app/manage.py test audit</verify>
  <acceptance_criteria>
    - `analyze_auto_sort` captures each `files` upload artifact.
    - `analyze_multiple` captures both `lunit_files` and `ground_truth_files`.
    - `precheck_files` captures uploaded files without breaking parsing.
    - Audit metadata includes artifact fields and no raw report text.
  </acceptance_criteria>
  <done>Analysis upload artifacts are captured and linked.</done>
</task>

<task type="auto">
  <name>Task 3: Capture import and manual GT uploads</name>
  <files>django-app/upload/views.py, django-app/gt/views.py, django-app/audit/tests.py</files>
  <read_first>django-app/upload/views.py, django-app/gt/views.py, django-app/audit/services.py</read_first>
  <action>Instrument `import_preview`, `import_confirm`, and `gt.validate_upload` so uploaded CSV/XLS/XLSX files create `FileUpload` audit events with artifact links. Metadata should include mode, row counts or validated row counts where available, mapping/count summaries, and filename/hash/size. Do not store row contents in audit metadata.</action>
  <verify>python django-app/manage.py test audit</verify>
  <acceptance_criteria>
    - Historical import preview/confirm uploads create FileUpload events.
    - Manual GT validate upload creates FileUpload event.
    - Metadata summarizes counts/mapping but not row contents.
  </acceptance_criteria>
  <done>Import and GT upload artifacts are captured and linked.</done>
</task>

</tasks>

<verification>
- [ ] `python django-app/manage.py check`
- [ ] `python django-app/manage.py test audit`
</verification>

<success_criteria>
- Upload files are copied to audit artifact storage.
- FileUpload events link to artifact path/hash/size.
- Existing upload, precheck, import, and GT parsing still work.
- Tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit-logging/01-03-SUMMARY.md`
</output>
