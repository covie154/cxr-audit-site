---
phase: 01-audit-logging
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - django-app/audit/services.py
  - django-app/audit/tests.py
  - django-app/upload/models.py
  - django-app/upload/migrations/0002_processingtask_initiated_by.py
  - django-app/upload/views.py
autonomous: true
requirements: [AUD-05, AUD-06, AUD-07, AUD-09, AUD-10]
user_setup: []
must_haves:
  truths:
    - "D-10 D-11 AUD-05: LLMCall audit events record safe batch metadata and hashes where available, without storing raw prompt or response artifacts."
    - "D-12 AUD-06: Successful LLM batches save result rows to a CSV audit artifact and link it from an LLMSuccess event."
    - "AUD-07: Failed LLM batches create LLMFailure events with task ID, safe error metadata, and no secrets/raw PHI."
    - "D-13 D-14: Background and LLM events are attributed to the original initiating user and include task ID and background timing metadata."
    - "D-25 D-26: Django owns all audit writes for FastAPI/LLM activity in Phase 1."
  artifacts:
    - path: "django-app/upload/models.py"
      provides: "Initiating user attribution on ProcessingTask"
      contains: "initiated_by"
    - path: "django-app/upload/views.py"
      provides: "LLMCall, LLMSuccess, and LLMFailure event integration"
      contains: "LLMSuccess"
  key_links:
    - from: "django-app/upload/views.py"
      to: "django-app/audit/services.py"
      via: "emit_audit_event and result CSV artifact helpers"
      pattern: "LLM(Call|Success|Failure)"
---

<objective>
Implement LLM call/result audit categories, background attribution, CSV artifacts, and full Phase 1 regression coverage.

Purpose: The LLM path processes radiology report ePHI and must be traceable without duplicating raw prompts/responses in audit artifacts.
Output: Initiating-user task attribution, LLMCall/LLMSuccess/LLMFailure audit events, successful result CSV artifacts, and end-to-end audit tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit-logging/01-CONTEXT.md
@.planning/phases/01-audit-logging/01-RESEARCH.md
@.planning/phases/01-audit-logging/01-01-SUMMARY.md
@.planning/phases/01-audit-logging/01-02-SUMMARY.md
@.planning/phases/01-audit-logging/01-03-SUMMARY.md
@django-app/upload/models.py
@django-app/upload/views.py
@django-app/audit/services.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve initiating user on ProcessingTask</name>
  <files>django-app/upload/models.py, django-app/upload/migrations/0002_processingtask_initiated_by.py, django-app/upload/views.py, django-app/audit/tests.py</files>
  <read_first>django-app/upload/models.py, django-app/upload/views.py</read_first>
  <action>Add nullable `initiated_by = ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name="processing_tasks")` to `ProcessingTask`. Create migration. Set `initiated_by=request.user` when tasks are created in `analyze_auto_sort` and `analyze_multiple`. Background audit events should use `task.initiated_by` when available.</action>
  <verify>python django-app/manage.py makemigrations --check --dry-run && python django-app/manage.py test audit</verify>
  <acceptance_criteria>
    - ProcessingTask has `initiated_by`.
    - Analyze task creation stores the initiating user.
    - Migration exists and makemigrations reports no pending changes.
  </acceptance_criteria>
  <done>Background tasks can be attributed to initiating users.</done>
</task>

<task type="auto">
  <name>Task 2: Add LLMCall and LLMFailure events</name>
  <files>django-app/upload/views.py, django-app/audit/tests.py</files>
  <read_first>django-app/upload/views.py, django-app/audit/services.py</read_first>
  <action>Emit `LLMCall` after successful task submission to FastAPI from `analyze_auto_sort` and `analyze_multiple`, with safe metadata: task ID, model identifier if available from settings/environment, endpoint category/base host without secret, uploaded file count, supplemental_steps, and initiating username/user ID. Emit `LLMFailure` when FastAPI submission fails, background status reports failed, result fetch fails, or result save raises. Never include API keys, raw reports, patient names, or response bodies containing PHI.</action>
  <verify>python django-app/manage.py test audit</verify>
  <acceptance_criteria>
    - Successful analyze submission emits LLMCall.
    - Failed submission/background failure emits LLMFailure.
    - Metadata includes task_id where available.
    - Metadata excludes secrets and raw PHI.
  </acceptance_criteria>
  <done>LLM attempt/failure events are emitted safely.</done>
</task>

<task type="auto">
  <name>Task 3: Add LLMSuccess result CSV artifacts and phase tests</name>
  <files>django-app/audit/services.py, django-app/upload/views.py, django-app/audit/tests.py</files>
  <read_first>django-app/upload/views.py, django-app/audit/services.py</read_first>
  <action>Add helper to write successful `csv_data` from FastAPI results to a timestamped audit artifact CSV with SHA-256/size metadata. Integrate it in `get_results` and `_background_fetch_and_save` after successful save to `CXRStudy`. Emit `LLMSuccess` with task ID, initiating user, new/skipped/processed counts, artifact link/hash/size, and safe metadata. Add tests covering LLMCall, LLMSuccess CSV artifact, LLMFailure, user/task attribution, no raw prompt/response artifacts, and no direct PHI in metadata.</action>
  <verify>python django-app/manage.py check && python django-app/manage.py makemigrations --check --dry-run && python django-app/manage.py test audit</verify>
  <acceptance_criteria>
    - Successful result handling writes a CSV audit artifact.
    - LLMSuccess links to artifact path/hash/size.
    - Background and user-triggered result paths both emit safe events.
    - Full audit test suite passes.
  </acceptance_criteria>
  <done>LLM success/failure audit coverage is complete and tested.</done>
</task>

</tasks>

<verification>
- [ ] `python django-app/manage.py check`
- [ ] `python django-app/manage.py makemigrations --check --dry-run`
- [ ] `python django-app/manage.py test audit`
</verification>

<success_criteria>
- LLMCall, LLMSuccess, and LLMFailure events are emitted.
- Successful result CSV artifacts are saved and linked.
- Background events use initiating user and task ID.
- No raw prompts/responses are stored as audit artifacts.
- Phase audit tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit-logging/01-04-SUMMARY.md`
</output>
