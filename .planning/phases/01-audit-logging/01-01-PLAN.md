---
phase: 01-audit-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - django-app/lunit_audit/settings.py
  - django-app/lunit_audit/dbrouters.py
  - django-app/audit/apps.py
  - django-app/audit/__init__.py
  - django-app/audit/models.py
  - django-app/audit/admin.py
  - django-app/audit/migrations/__init__.py
  - django-app/audit/migrations/0001_initial.py
  - django-app/audit/tests.py
autonomous: true
requirements: [AUD-01, AUD-08, AUD-09]
user_setup: []
must_haves:
  truths:
    - "D-01 D-02 AUD-01: AuditEvent stores a readable action string plus structured metadata JSON."
    - "D-03 D-04: AuditEvent schema supports PHI-avoiding fields and does not require patient names or raw report text."
    - "D-05: AuditEvent category choices include UserSelected, FileUpload, LLMCall, LLMSuccess, and LLMFailure."
    - "D-15 D-16 AUD-08: Django settings define a separate audit database alias using a second SQLite database for Phase 1 and a router ready for future PostgreSQL configuration."
    - "D-19 D-20: Settings expose configurable audit event and artifact retention days."
    - "D-21 D-23 AUD-09: AuditEvent has read-only admin visibility and application-level append-only behavior."
    - "D-22: Phase 1 does not build a full audit search/filter/export UI; it only provides minimal read-only admin visibility."
    - "D-24: DB-level append-only protections are deferred for reconsideration when PostgreSQL support lands."
  artifacts:
    - path: "django-app/audit/models.py"
      provides: "AuditEvent model with category, user, action, metadata, artifact, and integrity fields"
      contains: "class AuditEvent"
    - path: "django-app/lunit_audit/dbrouters.py"
      provides: "Database router for audit app"
      contains: "class AuditRouter"
    - path: "django-app/audit/admin.py"
      provides: "Read-only AuditEvent admin"
      contains: "has_delete_permission"
  key_links:
    - from: "django-app/lunit_audit/settings.py"
      to: "django-app/lunit_audit/dbrouters.py"
      via: "DATABASE_ROUTERS setting"
      pattern: "DATABASE_ROUTERS"
---

<objective>
Create the audit logging foundation.

Purpose: Phase 1 needs durable, separate audit storage before any workflow instrumentation can safely write events.
Output: A new Django `audit` app, a separate audit database alias, router, `AuditEvent` model, read-only admin visibility, retention settings, and foundational tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit-logging/01-CONTEXT.md
@.planning/phases/01-audit-logging/01-RESEARCH.md
@django-app/lunit_audit/settings.py
@django-app/upload/models.py
@django-app/upload/admin.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add audit app and settings</name>
  <files>django-app/lunit_audit/settings.py, django-app/lunit_audit/dbrouters.py, django-app/audit/apps.py, django-app/audit/__init__.py, django-app/audit/migrations/__init__.py</files>
  <read_first>django-app/lunit_audit/settings.py, django-app/upload/apps.py</read_first>
  <action>Add `audit.apps.AuditConfig` to `INSTALLED_APPS`. Add `AUDIT_ARTIFACT_ROOT`, `AUDIT_EVENT_RETENTION_DAYS`, and `AUDIT_ARTIFACT_RETENTION_DAYS` settings with environment overrides. Extend `DATABASES` with an `audit` alias using SQLite path `BASE_DIR / "db" / "audit.sqlite3"` unless overridden by environment. Add `DATABASE_ROUTERS = ["lunit_audit.dbrouters.AuditRouter"]`. Create `AuditRouter` so models with `app_label == "audit"` read/write/migrate only on the `audit` DB and non-audit app migrations do not run on the audit DB.</action>
  <verify>python django-app/manage.py check</verify>
  <acceptance_criteria>
    - `settings.py` contains `"audit.apps.AuditConfig"`.
    - `settings.py` contains an `audit` database alias.
    - `dbrouters.py` contains `class AuditRouter`.
    - `DATABASE_ROUTERS` references `lunit_audit.dbrouters.AuditRouter`.
  </acceptance_criteria>
  <done>Audit app and audit DB routing are configured.</done>
</task>

<task type="auto">
  <name>Task 2: Create AuditEvent model and migration</name>
  <files>django-app/audit/models.py, django-app/audit/migrations/0001_initial.py</files>
  <read_first>django-app/upload/models.py, .planning/phases/01-audit-logging/01-CONTEXT.md</read_first>
  <action>Create `AuditEvent` with category choices `UserSelected`, `FileUpload`, `LLMCall`, `LLMSuccess`, `LLMFailure`, and `AuditFailure`. Include timestamp, username, nullable user FK, action, metadata JSON, route, method, status_code, source_ip, task_id, accession_no, artifact_path, artifact_sha256, artifact_size, event_hash, previous_hash, created_at. Keep fields PHI-avoiding by design: no patient_name or report_text fields. Create migration manually or with `makemigrations audit`.</action>
  <verify>python django-app/manage.py makemigrations --check --dry-run</verify>
  <acceptance_criteria>
    - `AuditEvent` exists with category choices from D-05.
    - `AuditEvent` has `action` and `metadata` fields.
    - `AuditEvent` has artifact hash/path/size fields.
    - Migration creates the audit event table.
  </acceptance_criteria>
  <done>AuditEvent schema exists and migrations are up to date.</done>
</task>

<task type="auto">
  <name>Task 3: Add read-only admin and foundation tests</name>
  <files>django-app/audit/admin.py, django-app/audit/tests.py</files>
  <read_first>django-app/upload/admin.py, django-app/audit/models.py</read_first>
  <action>Register `AuditEvent` in Django admin with list display/search/filter suitable for internal review. Override add/change/delete permissions to prevent normal mutation. Add tests proving audit events write to the `audit` DB, normal DB does not receive audit events, category choices exist, and admin add/change/delete are disabled.</action>
  <verify>python django-app/manage.py test audit</verify>
  <acceptance_criteria>
    - `AuditEventAdmin` has no add/change/delete permissions.
    - Tests create an AuditEvent using `using("audit")`.
    - Tests pass with the audit database available.
  </acceptance_criteria>
  <done>Foundation tests pass and admin visibility is read-only.</done>
</task>

</tasks>

<verification>
- [ ] `python django-app/manage.py check`
- [ ] `python django-app/manage.py makemigrations --check --dry-run`
- [ ] `python django-app/manage.py test audit`
</verification>

<success_criteria>
- Audit model exists and can be migrated.
- Audit events route to a separate audit SQLite DB.
- Read-only admin visibility exists.
- Retention settings exist.
- Foundation tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit-logging/01-01-SUMMARY.md`
</output>
