---
phase: 01-audit-logging
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - django-app/audit/services.py
  - django-app/audit/middleware.py
  - django-app/audit/tests.py
  - django-app/lunit_audit/settings.py
  - django-app/viewer/views.py
  - django-app/report/views.py
  - django-app/gt/views.py
  - django-app/upload/views.py
autonomous: true
requirements: [AUD-01, AUD-02, AUD-09, AUD-10]
user_setup: []
must_haves:
  truths:
    - "D-01 D-02 AUD-01 AUD-02: Emitter accepts readable action strings plus structured metadata for routes, buttons, filters, date ranges, accession IDs, task IDs, filenames, and counts."
    - "D-03 D-04: Emitter sanitizes or hashes known PHI-like values and avoids patient names/raw report text in action strings and metadata."
    - "D-05 D-06 AUD-02: Authenticated GET/page interactions create UserSelected events with route and parameter details where present."
    - "D-17 D-18: Audit write failures do not block user workflows and are logged as telemetry without recursive audit writes."
    - "D-23: Normal application paths do not update/delete AuditEvent rows."
  artifacts:
    - path: "django-app/audit/services.py"
      provides: "Non-blocking audit emitter, metadata sanitizer, artifact helpers"
      contains: "def emit_audit_event"
    - path: "django-app/audit/middleware.py"
      provides: "UserSelected GET/page interaction logging"
      contains: "UserSelected"
  key_links:
    - from: "django-app/lunit_audit/settings.py"
      to: "django-app/audit/middleware.py"
      via: "MIDDLEWARE setting"
      pattern: "audit.middleware"
---

<objective>
Implement the reusable audit emitter and broad user-action instrumentation.

Purpose: Once the audit foundation exists, the app needs a safe write API and coverage for authenticated page/API interactions that expose or mutate ePHI.
Output: Non-blocking audit service helpers, `UserSelected` middleware, explicit view instrumentation for high-risk actions, and regression tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit-logging/01-CONTEXT.md
@.planning/phases/01-audit-logging/01-RESEARCH.md
@.planning/phases/01-audit-logging/01-01-SUMMARY.md
@django-app/audit/models.py
@django-app/lunit_audit/settings.py
@django-app/upload/views.py
@django-app/viewer/views.py
@django-app/report/views.py
@django-app/gt/views.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add non-blocking audit service helpers</name>
  <files>django-app/audit/services.py, django-app/audit/tests.py</files>
  <read_first>django-app/audit/models.py, .planning/phases/01-audit-logging/01-CONTEXT.md</read_first>
  <action>Create `emit_audit_event()` that writes `AuditEvent` using the `audit` DB alias and returns the event or `None`. It must catch exceptions, log telemetry with Python logging, and never raise to the caller. Add helpers for client IP extraction, safe metadata sanitization, hashing PHI-like or long values, and event hash calculation. Include an `AuditCategory` convenience class or constants matching model choices.</action>
  <verify>python django-app/manage.py test audit</verify>
  <acceptance_criteria>
    - `emit_audit_event` writes to `AuditEvent.objects.using("audit")`.
    - Tests simulate audit DB write failure and verify no exception escapes.
    - Sanitizer avoids raw values for keys like `patient_name`, `patient_id`, `text_report`, `report`, `q`.
  </acceptance_criteria>
  <done>Reusable non-blocking emitter is tested.</done>
</task>

<task type="auto">
  <name>Task 2: Add UserSelected middleware</name>
  <files>django-app/audit/middleware.py, django-app/lunit_audit/settings.py, django-app/audit/tests.py</files>
  <read_first>django-app/lunit_audit/settings.py, django-app/lunit_audit/urls.py</read_first>
  <action>Add middleware after authentication middleware that emits `UserSelected` for authenticated GET requests except admin static/media and audit admin endpoints as needed to avoid noise/recursion. Action should include method and path; metadata should include sanitized query parameters, resolved route name if available, status code, and source IP. Add middleware to settings.</action>
  <verify>python django-app/manage.py test audit</verify>
  <acceptance_criteria>
    - Authenticated GET request creates `UserSelected`.
    - Anonymous GET request does not create audit event.
    - Query parameters are sanitized and do not include direct PHI-like values.
  </acceptance_criteria>
  <done>UserSelected middleware is active and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Instrument representative high-risk actions</name>
  <files>django-app/viewer/views.py, django-app/report/views.py, django-app/gt/views.py, django-app/upload/views.py, django-app/audit/tests.py</files>
  <read_first>django-app/viewer/views.py, django-app/report/views.py, django-app/gt/views.py, django-app/upload/views.py</read_first>
  <action>Add explicit audit calls for viewer detail/update/delete/bulk-delete/export, report generation/export/email/PDF, manual GT report-count/download/validate/apply, task delete/cancel/status/results, import preview/confirm, API/LLM health checks, active task checks, and saved task results. Use `UserSelected` for view/select/download/export actions unless a later plan provides `FileUpload` or LLM categories. Include route, method, date ranges, accession/task IDs, counts, field names, and sanitized filters. Do not include patient names or raw text_report/report_data bodies.</action>
  <verify>python django-app/manage.py test audit</verify>
  <acceptance_criteria>
    - Viewer detail/update/delete/export paths emit audit events.
    - Report date-range actions include date_from/date_to in metadata.
    - GT download/apply actions include start/end/count or update counts.
    - No explicit action metadata includes raw report text.
  </acceptance_criteria>
  <done>Representative high-risk workflows emit audit events.</done>
</task>

</tasks>

<verification>
- [ ] `python django-app/manage.py check`
- [ ] `python django-app/manage.py test audit`
</verification>

<success_criteria>
- Non-blocking audit emitter exists.
- Broad authenticated GET coverage exists.
- High-risk ePHI workflows emit explicit audit events.
- PHI-like metadata is sanitized.
- Tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit-logging/01-02-SUMMARY.md`
</output>
