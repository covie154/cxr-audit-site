---
phase: 02-transmission-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - django-app/lunit_audit/settings.py
  - django-app/lunit_audit/checks.py
  - django-app/lunit_audit/apps.py
  - django-app/lunit_audit/tests.py
  - nginx/nginx.conf
requirements: [SEC-01, SEC-04, SEC-05]
autonomous: true
must_haves:
  truths:
    - "D-05 SEC-01: Production Django settings enforce HTTPS-aware cookies, redirects, HSTS, proxy SSL handling, and security headers."
    - "D-08 SEC-04: Production LLM endpoint transport risk is surfaced by an executable Django check."
    - "D-09 SEC-05: mTLS is documented as a future hardening option, not implemented in Phase 2."
---

<objective>
Harden Django/Nginx production security settings and add practical LLM transport validation.
</objective>

<tasks>
<task type="auto">
  <name>Add production security settings and system checks</name>
  <files>django-app/lunit_audit/settings.py, django-app/lunit_audit/checks.py, django-app/lunit_audit/apps.py, django-app/lunit_audit/tests.py</files>
  <action>Add environment-driven HSTS/SSL settings and a Django system check that warns when production LLM transport uses HTTP without explicit private-transport allowance. Ensure the app config imports checks.</action>
  <verify>python django-app/manage.py check</verify>
</task>
<task type="auto">
  <name>Document mTLS and tighten Nginx headers</name>
  <files>nginx/nginx.conf, documentation/HIPAA-plan-phase-2.md</files>
  <action>Keep Nginx as the HTTPS edge, ensure security headers are always applied where appropriate, and record mTLS as deferred production hardening.</action>
  <verify>docker compose config</verify>
</task>
</tasks>

<verification>
- [ ] `python django-app/manage.py check`
- [ ] `python django-app/manage.py test lunit_audit`
- [ ] `docker compose config`
</verification>
