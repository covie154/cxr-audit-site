---
phase: 02-transmission-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cxr-audit-api/combined_server.py
  - cxr-audit-api/tests/test_auth.py
  - docker-compose.yml
  - .env.example
requirements: [SEC-02]
autonomous: true
must_haves:
  truths:
    - "D-01 D-02 SEC-02: FastAPI rejects production calls when auth is required and API_SECRET_KEY is missing or invalid."
    - "D-03: A non-PHI /health endpoint remains available for container health checks."
    - "D-07: FastAPI CORS origins are environment-driven and not wildcard in production."
---

<objective>
Make FastAPI service authentication fail closed in production and update health/CORS behavior.
</objective>

<tasks>
<task type="auto">
  <name>Implement FastAPI fail-closed auth</name>
  <files>cxr-audit-api/combined_server.py, cxr-audit-api/tests/test_auth.py</files>
  <action>Add production auth requirement detection, 503 on missing required secret, 403 on invalid key, unauthenticated /health, and CORS origin settings from FASTAPI_CORS_ORIGINS.</action>
  <verify>python -m unittest discover -s cxr-audit-api/tests</verify>
</task>
<task type="auto">
  <name>Update Compose and environment documentation</name>
  <files>docker-compose.yml, .env.example</files>
  <action>Point API healthcheck at /health and document API_REQUIRE_AUTH plus CORS origins.</action>
  <verify>docker compose config</verify>
</task>
</tasks>

<verification>
- [ ] `python -m unittest discover -s cxr-audit-api/tests`
- [ ] `docker compose config`
</verification>
