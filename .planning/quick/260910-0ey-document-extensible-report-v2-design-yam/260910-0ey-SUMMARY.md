---
status: complete
---
# Report v2 planning pack

Commit: 7433e4f. Created .planning/report-v2 with design, five-stage plan, 19-task model-oriented
execution runbook, YAML contract/schema, 17-widget legacy-layout draft, threshold example and parity map.
User requested detailed instructions suitable for Qwen/Terra or equivalent; runbook includes task
ownership, dependencies, acceptance checks, hand-calculated synthetic fixtures and a handoff prompt.

Validation passed: JSON Schema syntax and report example, unique widget IDs, comparison defaults,
weekly buckets, policy reference, no YAML aliases, ten default finding thresholds, local Markdown links,
19 task IDs and git diff --check. No clinical records read, no production code changed, no email sent.

Documented legacy balanced-accuracy mislabel and box quartile inconsistency. Example thresholds copy
current defaults only; future user threshold changes are not inferred. Actual application remains the
existing v2 shell pending implementation. Existing HIPAA roadmap unchanged.
